# 🧪 دليل الاختبار - الأدوار الجديدة

## 🎯 كيفية اختبار الأدوار الجديدة

قبل أن تطلق، اتبع هذه الخطوات للتأكد من أن كل شيء يعمل بشكل صحيح.

---

## 1️⃣ إعداد بيئة الاختبار

### الخطوة الأولى: تشغيل المشروع
```bash
cd /workspaces/albyt
npm run dev
```
سيفتح على: `http://localhost:5173`

### الخطوة الثانية: نشر قواعس Firestore
```bash
firebase deploy --only firestore:rules
```

### الخطوة الثالثة: فتح Firebase Console
```
https://console.firebase.google.com/project/albayt-sofra/firestore
```

---

## 2️⃣ اختبار دور المشرف (Admin)

### المرحلة A: إنشاء حساب اختبار

1. **اذهب إلى `/register`**
   - البريد: `admin-test@example.com`
   - كلمة السر: `AdminTest@123`
   - الاسم: `مشرف الاختبار`

2. **تعديل الدور في Firebase**
   - اذهب إلى Firestore Console
   - اختر: `users` → `{uid}` (الـ uid الذي ظهر)
   - غيّر: `role: "admin"`
   - احفظ

3. **تحديث الصفحة**
   - أعد تحميل المتصفح
   - يجب أن تنقلك تلقائياً إلى `/`

### المرحلة B: اختبار الصفحات

#### 1. لوحة التحكم (`/admin`)
```
✅ اختبر:
  [ ] اضغط على "لوحة التحكم"
  [ ] تظهر الإحصائيات
  [ ] يظهر رصيد المحفظة
  [ ] تظهر القوائم السريعة
  [ ] لا توجد رسائل خطأ في Console
```

**ما يجب أن تراه:**
- 💰 رصيد المحفظة (0 أو أكثر)
- 📊 عدد المطاعم
- 📋 عدد الطلبات
- ⏳ الطلبات المعلقة

#### 2. إدارة المطاعم (`/admin/restaurants`)
```
✅ اختبر:
  [ ] اضغط على "إدارة المطاعم"
  [ ] يظهر نموذج إضافة مطعم
  [ ] أدخل اسم مطعم واختبر الإضافة
  [ ] يظهر المطعم في القائمة
  [ ] زر الحذف موجود ويعمل
  [ ] لا توجد أخطاء في Firestore
```

**نموذج الاختبار:**
```
اسم المطعم: "مطعم البرسيم"
الهاتف: "0555555555"
المدينة: "الرياض"
الموقع: "حي العليا"
```

**التحقق:**
- ✅ ظهور رسالة نجاح
- ✅ إضافة المطعم إلى القائمة
- ✅ حفظ في Firestore

#### 3. مراقبة الطلبات (`/admin/orders`)
```
✅ اختبر:
  [ ] اضغط على "مراقبة الطلبات"
  [ ] تظهر أزرار التصفية
  [ ] يمكن التصفية حسب الحالة
  [ ] عرض الطلبات بشكل صحيح
  [ ] عدم وجود زر تعديل على الطلبات
  [ ] الأسعار تظهر بشكل صحيح
```

**ما يجب أن تتحقق منه:**
- ✅ لا يوجد زر "تعديل"
- ✅ لا يوجد زر "حذف"
- ✅ يمكن فقط العرض
- ✅ التفاصيل كاملة

#### 4. صلاحيات العميل
```
✅ اختبر:
  [ ] اضغط على "استعرض القائمة"
  [ ] تظهر قائمة المطاعم
  [ ] يمكن إضافة إلى السلة
  [ ] اذهب إلى `/checkout`
  [ ] يمكن إكمال عملية الطلب
  [ ] تظهر "طلباتي" في الملف الشخصي
```

### المرحلة C: التحقق من البيانات

#### تحقق من Firestore:
```
firestore > wallets > {adminId}
  ├─ balance: (أي رقم ≥ 0)
  ├─ earnings: (مجموع الأرباح)
  └─ rewards: (المكافآت)
```

#### تحقق من Console:
```javascript
// في DevTools → Console
// لا يجب أن تظهر أخطاء حمراء
// فقط تحذيرات عادية ممكنة
```

---

## 3️⃣ اختبار دور المطور (Developer)

### المرحلة A: إنشاء حساب اختبار

1. **أنشئ حساب جديد**
   - البريد: `developer-test@example.com`
   - كلمة السر: `DevTest@123`
   - الاسم: `مطور الاختبار`

2. **تعديل الدور**
   - اذهب إلى Firestore
   - اختر: `users` → `{uid}`
   - غيّر: `role: "developer"`
   - احفظ

3. **تحديث الصفحة**

### المرحلة B: اختبار الصفحة

#### لوحة المطور (`/developer`)
```
✅ اختبر:
  [ ] اضغط على `/developer`
  [ ] تظهر البيانات الشخصية
  [ ] يظهر البريد الإلكتروني والـ UID
  [ ] تظهر الخيارات الرئيسية
  [ ] تظهر المنطقة الخطرة
  [ ] الروابط تعمل بشكل صحيح
```

**ما يجب أن تراه:**
```
👨‍💻 المطور: مريم الكناني
📧 البريد: developer-test@example.com
🔐 الـ UID: {المعرف الفريد}

🎯 القوائم:
├─ 👥 إدارة المستخدمين (Coming Soon)
├─ 🏪 إدارة المطاعم
├─ 📦 إدارة الطلبات
├─ ⚙️ الإعدادات (Coming Soon)
├─ 📊 الإحصائيات (Coming Soon)
└─ 📝 السجلات (Coming Soon)

⚠️ المنطقة الخطرة:
├─ 🗑️ حذف الحساب
└─ 🗑️ حذف المطعم
```

#### الروابط المتاحة:
```
✅ اختبر:
  [ ] رابط "إدارة المطاعم" → يذهب إلى /admin/restaurants
  [ ] رابط "إدارة الطلبات" → يذهب إلى /admin/orders
  [ ] يمكن الوصول لكل الصفحات الإدارية
```

#### المنطقة الخطرة:
```
✅ اختبر:
  [ ] انقر على "حذف الحساب"
  [ ] يظهر تأكيد
  [ ] انسخ الكود المطلوب
  [ ] ألصقه للتأكيد
  [ ] حسناً - ألغِ (لا تحذف فعلياً الآن!)
```

---

## 4️⃣ اختبار قواعس Firestore

### أولاً: تأكد من نشر القواعس
```bash
firebase deploy --only firestore:rules
# يجب أن يقول: ✓ Rules updated
```

### ثانياً: اختبر الصلاحيات

#### اختبر كمشرف:
```javascript
// Admin يجب أن يستطيع:
✅ قراءة كل الطلبات (read)
✅ إضافة مطاعم (create in restaurants)
✅ إدارة محفظته (update wallets/{uid})
❌ تعديل الطلبات (update in orders) → يجب أن يفشل
❌ حذف الطلبات (delete in orders) → يجب أن يفشل
```

#### اختبر كمطور:
```javascript
// Developer يجب أن يستطيع:
✅ كل شيء (جميع العمليات)
✅ إضافة مستخدمين
✅ حذف مطاعم
✅ تعديل الطلبات
✅ إدارة الإعدادات
```

---

## 5️⃣ اختبار الأخطاء

### افتح DevTools (F12)
```
Chrome/Firefox → F12 → Console
```

### لا يجب أن تظهر أخطاء حمراء:
```javascript
❌ Uncaught (in promise) FirebaseError
❌ Cannot read property 'xyz' of undefined
❌ TypeError: xyz is not a function
```

### ممكن أن تظهر تحذيرات:
```javascript
⚠️ Non-Error promise rejection captured
⚠️ React requires useCallback dependency
```

---

## 6️⃣ قائمة التحقق الشاملة

### 🔷 اختبار Admin:
- [ ] الدخول بحساب admin
- [ ] تظهر `/admin`
- [ ] تظهر الإحصائيات
- [ ] يمكن إضافة مطعم
- [ ] يمكن عرض الطلبات
- [ ] لا يمكن تعديل الطلبات
- [ ] تظهر المحفظة
- [ ] يمكن الطلب كعميل
- [ ] لا توجد أخطاء في Console

### 🔷 اختبار Developer:
- [ ] الدخول بحساب developer
- [ ] تظهر `/developer`
- [ ] تظهر البيانات الشخصية
- [ ] تظهر القوائم الرئيسية
- [ ] يمكن الوصول للقوائم الفرعية
- [ ] تظهر المنطقة الخطرة
- [ ] يمكن الوصول `/admin/restaurants`
- [ ] يمكن الوصول `/admin/orders`
- [ ] لا توجد أخطاء في Console

### 🔷 اختبار Firestore:
- [ ] القواعس منشورة
- [ ] Admin لا يقدر يعدّل الطلبات
- [ ] Developer يقدر يعدّل الطلبات
- [ ] الـ wallets محفوظة بشكل صحيح

### 🔷 اختبار الأمان:
- [ ] لا يمكن الوصول `/admin` بدون admin
- [ ] لا يمكن الوصول `/developer` بدون developer
- [ ] لا يمكن تغيير role من التطبيق
- [ ] Firestore يفرض الصلاحيات

---

## 7️⃣ حل المشاكل الشائعة

### المشكلة: لا تظهر الصفحة
**الحل:**
1. تحقق من أن الدور صحيح في Firestore
2. حاول تحديث الصفحة (F5)
3. امسح localStorage: `localStorage.clear()`
4. تحقق من Console للأخطاء

### المشكلة: أخطاء Firestore
**الحل:**
1. تأكد من نشر القواعس: `firebase deploy --only firestore:rules`
2. تحقق من الـ UID في الـ URL
3. افتح Firestore Console وتحقق من البيانات
4. ارجع إلى `firebase.rules` وتحقق من الصيغة

### المشكلة: لا يمكن إضافة مطعم
**الحل:**
1. تأكد أنك مسجل كـ admin
2. تحقق من دخول اسم المطعم
3. افتح Console وانسخ الخطأ
4. تحقق من Firestore rules للـ create

### المشكلة: الطلبات لا تظهر
**الحل:**
1. تأكد من وجود طلبات في Firestore
2. تحقق من الفلتر المختار
3. افتح DevTools وتحقق من الـ query
4. حاول تحديث الصفحة

---

## 8️⃣ الاختبار النهائي

### قبل الإطلاق الكامل:
```
✅ Checklist:
[ ] Admin يدخل ويرى Dashboard
[ ] Admin يضيف مطعم بنجاح
[ ] Admin يرى الطلبات ولا يعدّلها
[ ] Admin يرى المحفظة
[ ] Admin يطلب كعميل
[ ] Developer يدخل ويرى الأدوات
[ ] Developer يفتح الروابط
[ ] لا توجد أخطاء حمراء في Console
[ ] Firestore rules منشورة
[ ] البناء يعمل بدون أخطاء
```

---

## 9️⃣ تقرير الاختبار

### بعد انتهائك من الاختبار:
أنشئ ملف `TEST_REPORT.md` وسجّل:
```markdown
# تقرير الاختبار

## التاريخ: [تاريخ اليوم]

### Admin Role: ✅ نجح / ❌ فشل
- [ ] لوحة التحكم
- [ ] إدارة المطاعم
- [ ] مراقبة الطلبات
- [ ] المحفظة
- [ ] صلاحيات العميل

### Developer Role: ✅ نجح / ❌ فشل
- [ ] الصفحة الرئيسية
- [ ] الروابط
- [ ] المنطقة الخطرة

### Firestore Rules: ✅ نجح / ❌ فشل
- [ ] Admin لا يعدّل الطلبات
- [ ] Developer يعدّل الطلبات

### ملاحظات:
[أي مشاكل أو ملاحظات]
```

---

**آخر تحديث**: December 24, 2025  
**جاهز للاختبار**: ✅ نعم
